/**
 * PostCSS configuratie voor Tailwind CSS processing.
 * Gebruikt de nieuwe @tailwindcss/postcss plugin voor Tailwind CSS v4.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;

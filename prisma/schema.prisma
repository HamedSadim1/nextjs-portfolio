// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider   = "prisma-client-js"
  engineType = "library"
}

datasource db {
  provider = "sqlite"
}

model Project {
  id          String   @id @default(cuid())
  imageUrl    String
  title       String
  description String
  liveUrl     String
  tags        Json // JSON array voor tags - beter voor query performance en type safety
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("projects")
}

model User {
  id          String   @id @default(cuid())
  name        String
  avatarUrl   String
  bio         String
  description Json // JSON array voor descriptions
  socials     Json // JSON voor social links
  createdAt   DateTime @default(now())
}

model SkillCategory {
  id        String   @id @default(cuid())
  title     String
  order     Int // Voor sortering van categorieën
  skills    Skill[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("skill_categories")
}

model Skill {
  id         String        @id @default(cuid())
  name       String
  category   SkillCategory @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  categoryId String
  createdAt  DateTime      @default(now())
  updatedAt  DateTime      @updatedAt

  @@map("skills")
}
